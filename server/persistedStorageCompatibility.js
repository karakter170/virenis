// Transitional server import path. The pure shared compatibility contract is
// also consumed by the browser-side editor for existing persisted records.
export * from "../shared/persistedStorageCompatibility.js";
